# Fail fast when a dependency is missing from package.json.
strict-peer-dependencies=false
# The published package bundles everything, so nothing here reaches users.
auto-install-peers=true
resolution-mode=highest
